import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("RoundTimer")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}

#Preview {
    ContentView()
}
